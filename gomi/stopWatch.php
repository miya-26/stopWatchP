<!DOCTYPE html>
<html lang='ja'>
    <head>
        <meta charset='UTF-8'>
        <title>時間を図る</title>
    <body>
    <?php
    // paizaのやつ
    /*$input = fgets(STDIN);    //標準入力から受け取る
    $input2 = fgets(STDIN);
    echo "出力：" .$input ."\n";
    echo "出力：" .$input2 ."\n";
    date_default_timezone_set('japan');  //タイムゾーン設定
    //echo date('Y/m/d H:i:s');    //時刻取得

    if ($input == 'start'){
        date('Y/m/d H:i:s');
        $un = date('Y/m/d H:i:s'); 
        echo "現在時刻：" .$un ."\n";
        echo "勉強スタート！\n";
        if ($input2 == 'stop') {
            $deux = date('Y/m/d H:i:s');
            echo "現在時刻：".$deux ."\n";
            echo "分勉強しました！";
    }else{
        echo "無効な入力";
    }
    }
    */
    ?> 
    </body>
    </head>
</html>

